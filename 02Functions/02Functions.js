// Array of type string
var cities = ["Delhi", "Banglore", "Pune", "Noida"];
// mapping over an array
//  method-1
// type any
cities.map(function (city) {
    return "".concat(city, " belong to India");
});
// method-2
// varibale type define
cities.map(function (city) {
    return "".concat(city, " belong to India");
});
// method-3 
// function return type define 
cities.map(function (city) {
    return "".concat(city, " belong to India");
});
